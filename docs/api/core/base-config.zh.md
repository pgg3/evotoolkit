# BaseConfig（配置基类）

::: evotoolkit.core.BaseConfig

---

常见参数：

- `interface`（`BaseMethodInterface`）：任务接口
- `output_path`（`str`）：结果保存路径
- `running_llm`（`HttpsApi`）：LLM 客户端
- `max_generations`（`int`）：最大代数
- `pop_size`（`int`）：种群大小
