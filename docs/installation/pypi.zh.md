# 从 PyPI 安装

通过 pip 安装 EvoToolkit 是最简单的方式。

---

## 基本安装

```bash
pip install evotoolkit
```

---

## 可选依赖

EvoToolkit 为特定任务提供可选的附加依赖。例如，要启用 CUDA 内核工程类任务：

```bash
pip install evotoolkit[cuda_engineering]
```

